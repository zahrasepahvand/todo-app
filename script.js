function addTask() {    let taskInput = document.getElementById("taskInput");    let taskText = taskInput.value.trim();        if (taskText === "") return; // Hinzufügen leerer Aufgaben verhindern    let li = document.createElement("li");    li.innerHTML = `${taskText} <button onclick="removeTask(this)">X</button>`;        li.addEventListener("click", function() {        li.classList.toggle("done"); // Durchstreichen der erledigten Arbeit.    });    document.getElementById("taskList").appendChild(li);    taskInput.value = ""; // Löschen eines Eintrags nach Hinzufügen}function removeTask(button) {       taskText.classList.toggle("done");}